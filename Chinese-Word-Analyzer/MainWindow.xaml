<Window x:Class="Chinese_Word_Analyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chinese_Word_Analyzer"
        mc:Ignorable="d"
        Title="{DynamicResource ProgramName}" Height="545" Width="740">
    <Window.Resources>
        <Style x:Key="SeparatorStyle" TargetType="{x:Type Separator}">
            <Setter Property="Background" Value="LightGray"/>
        </Style>
    </Window.Resources>
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="{DynamicResource File}" Height="25">
                <MenuItem Header="{DynamicResource File.Open}" />
                <MenuItem Header="{DynamicResource File.Save}" />
                <MenuItem Header="{DynamicResource File.Copy}"/>
                <Separator Style="{StaticResource  SeparatorStyle}"/>
                <MenuItem x:Name="FileInfo" IsEnabled="False" Header="{DynamicResource File.Info}"/>
            </MenuItem>
            <MenuItem Header="{DynamicResource More}">
                <MenuItem Header="{DynamicResource More.BatchConversion}" />
                <Separator Style="{StaticResource  SeparatorStyle}"/>
                <MenuItem Header="{DynamicResource More.OnlineSupport}" />
                <MenuItem Header="{DynamicResource More.About}"/>
                <Separator Style="{StaticResource  SeparatorStyle}"/>
                <MenuItem Header="{DynamicResource More.Language}">
                    <MenuItem x:Name="enUS" Header="English" IsCheckable="True"/>
                    <MenuItem x:Name="zhCHS" Header="简体中文" IsCheckable="True"/>
                    <MenuItem x:Name="zhCHT" Header="繁體中文" IsCheckable="True"/>
                    <Separator Style="{StaticResource  SeparatorStyle}"/>
                    <MenuItem Header="{DynamicResource More.Language.UseSystemLanguage}"/>
                </MenuItem>
            </MenuItem>
        </Menu>
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{DynamicResource StatusBar.Ready}"/>
            </StatusBarItem>
            <Separator Style="{StaticResource  SeparatorStyle}"/>
            <StatusBarItem>
                <ProgressBar Width="100" Height="18" Name="MainProcessBar"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
